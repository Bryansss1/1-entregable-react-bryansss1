import { useState } from "react";
import Cardcam from "./Cardcam";
import quotesss from "../src/quotes"

const Button = ({cambio}) => {
    return (
        <>
            <button onClick={cambio}>💓</button>
        </>
    );
};

export default Button;